Источники: CASE.md, tests_integration.md, tests_unit.md (прикреплены, других нет).
Не редактируй файлы, не предлагай патч. Отвечай на русском.

Найди в этих файлах явно зафиксированные числа по нагрузке: лимиты размера
diff, таймауты, RPS, конкурентность, SLA по задержке. Не придумывай и не
усредняй — только то, что написано прямо.

Формат ответа, две секции:

## Подтверждённые числа
<число> — <смысл> | Evidence: FILE.md:строки

## Категории без подтверждения
<категория> — источника нет

Каждая из пяти категорий должна попасть в одну из двух секций — пропускать нельзя.