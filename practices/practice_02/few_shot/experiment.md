# Few-shot

- Артефакт Практики 1: `tests_load.md`
- Что хотим улучшить: в таблице только один сценарий нагрузки без обоснования цифр и без проверки границ правил API-1 и REL-1 под нагрузкой. 

## Примеры

### Хороший результат

Строка из `tests_integration.md`:
`API-1; app/api.py:8–10` - конкретный файл и строка, измеримый ожидаемый результат («HTTP 413 и сообщение о лимите»), Evidence ссылается на правило репозитория.

### Плохой результат

`Проверяем нагрузку, ожидаем что всё будет работать нормально` - нет ссылки на правило, нет измеримого критерия, Evidence пустой.

## Запрос

[prompt.md](prompt.md)

## Что получили

Изменение в test_load.md зафиксированы в [result_test_load.md](result_test_load.md).

| Граница размера diff (API-1) | 50 запросов/с, 2 минуты; 50% запросов с diff 20k, 50% — 21k | 20k: 0% ошибок; 21k: 100% HTTP 413 | Доли 200/413 и P95 времени ответа | API-1; app/api.py:8–10 |
| Граница таймаута (REL-1) | Смоделировать LLM, отвечающий 9.5–11с; 20 запросов/с, 2 минуты | >10с: 100% контролируемых ответов без подвисания | Доля контролируемых таймаутов, отсутствие висячих запросов | REL-1; app/review_service.py:14–16 |

## Что изменили в исходном артефакте

- Файл и раздел: `tests_load.md`, таблица сценариев (конец таблицы, после существующей строки).
- Изменение: добавили 2 строки:
  1. «Граница размера diff (API-1)» — Evidence: `API-1; app/api.py:8–10`.
  2. «Граница таймаута (REL-1)» — Evidence: `REL-1; app/review_service.py:14–16`.
- Как проверили: каждая новая строка содержит ссылку на конкретное правило из CASE.md (API-1, REL-1) и файл:строку, уже использованные в других тестовых файлах Практики 1 (`tests_integration.md`, `tests_unit.md`); формат строки (5 колонок) совпадает с существующей таблицей.
- Что отклонили: ничего — модель вернула ровно 2 строки, обе подтверждены правилами и приняты без изменений.