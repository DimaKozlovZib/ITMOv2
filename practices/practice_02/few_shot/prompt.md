Контекст: правила репозитория (CASE.md):
- API-1: diff длиннее 20 000 символов отклоняется с HTTP 413.
- REL-1: внешний LLM-вызов имеет timeout 10 секунд; ошибка превращается в
  контролируемый ответ.

Целевой файл: tests_load.md. Не редактируй никакие другие файлы (в частности
prompt.md, experiment.md не трогай вообще).

Текущий формат таблицы в tests_load.md — 5 колонок:
| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |

Хороший пример полной строки (стиль tests_integration.md):
| Diff > лимита API-1 | Отправить diff длиной 21k | HTTP 413 и сообщение о лимите | Корректность отклонения | API-1; app/api.py:8–10 |

Плохой пример: "Проверяем нагрузку, ожидаем что всё будет работать нормально"
— не таблица, нет правила, нет измеримого критерия.

Задача: открой tests_load.md и добавь в конец существующей таблицы 2, которые проверяют поведение системы под нагрузкой именно на
границах правил API-1 и REL-1. Не добавляй сценарии, не подтверждённые этими
двумя правилами.

Формат ответа: выведи только 2 готовые строки таблицы целиком (со всеми 5
колонками), без пояснений вне таблицы и без изменений где-либо ещё.