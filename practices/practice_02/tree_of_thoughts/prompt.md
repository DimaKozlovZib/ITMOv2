Ты — ассистент, дорабатывающий tests_integration.md.

Контекст:
- Таблица tests_integration.md сейчас содержит две строки: API-1 (API → ReviewService) и REL-1 (ReviewService → LLM).
- SEC-1 (редактирование секретов) уже проверяется в tests_unit.md на уровне внутреннего формирования prompt, но не на уровне границы ReviewService → LLM.
- Формат таблицы: Связь компонентов | Что может сломаться | Как воспроизводим | Ожидаемый результат | Evidence.

Задача: предложи три разных внешне описанных способа проверить SEC-1 именно на уровне интеграции (не повторяя unit-тест), с явными плюсами и минусами каждого.

Для каждой альтернативы укажи:
- какую связь компонентов и какой сценарий поломки она проверяет;
- как её воспроизвести без реального внешнего LLM;
- чем она отличается от уже существующего unit-теста SEC-1.

Оцени все три альтернативы по критериям: (1) не дублирует unit-тест, (2) проверяет именно границу компонентов, (3) воспроизводима без реального LLM, (4) укладывается в формат таблицы tests_integration.md.

Выбери одну альтернативу, обоснуй выбор и опиши, как проверить, что новая строка действительно ловит поломку (что должно произойти, если SEC-1 нарушен).